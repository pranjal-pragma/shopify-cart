{
  "name": "shopify-cart-app",
  "private": true,
  "packageManager": "npm@10.9.8",
  "scripts": {
    "build": "npm --prefix admin run build",
    "dev": "uv run python scripts/run_shopify_dev.py"
  }
}
